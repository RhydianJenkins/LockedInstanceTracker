# WoW - Locked Instance Tracker

A simple World of Warcraft addon to show locked hc instances when the player opens the dungeon finder window.

Use the following command to toggle the interface:

```
/locks
```
